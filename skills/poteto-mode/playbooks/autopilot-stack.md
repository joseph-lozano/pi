### Autopilot-stack

> **Pi status:** Unavailable in this port.

The upstream workflow requires Graphite and cloud-agent lifecycle primitives. Do not select it while `gt` is absent.

Use a parent-owned Multi-phase plan with local workers in isolated worktrees. Build and verify locally, preserve the intended dependency order in the report, and stop before any external action.
